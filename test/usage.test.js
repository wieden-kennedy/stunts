describe('general usage', () => {
    it('is a place to quickly test ideas with assertions', () => {
        console.log('run with `npm test -- --watch`');
        expect(false).toBe(true);
    });
});
